#include <stdio.h>

int main(void)
{
	int i, j,t, r;
	char T[20];
	scanf("%d%*[^\n]", &t);

	while (t--) {
		scanf("%d %[^\n]s", &r, T);
		for (i = 0; T[i]!='\0'; i++) {
			for (j = 0; j < r; j++)
				printf("%c", T[i]);
		}
		printf("\n");
	}
	return 0;
}

#include <stdio.h>

int main() {
	int i=10, j=0;
	char str[101];
	scanf("%[^\n]", str);
	fflush(stdin);
	for (i = 10; str[j] != '\0'; i += 10){
		for (; j < i && (str[j] != '\0'); j++)
			putchar(str[j]);
		printf("\n");
	}
	return 0;
}

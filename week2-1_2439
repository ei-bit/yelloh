#include <stdio.h>

int main() {
	int a, i, j;
	scanf("%d", &a); j = a;
	do {
		for (i = a; i > 1; i--) {
			printf(" ");
		}
		for (i = 0; i < j-a+1 ; i++) {
			printf("*");
		}
		printf("\n");
	} while ((--a) != 0);
	return 0;
}

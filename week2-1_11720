#include <stdio.h>

int main() {
	int x, i=0, j = 0;
	char str[101];
	scanf("%d\n", &x);
	scanf("%[^\n]", str);
	fflush(stdin);
	for(i=0 ; i< x; i++)
		j += str[i] - '0';

	printf("%d\n", j);
	return 0;
}
